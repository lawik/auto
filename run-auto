#!/bin/bash
set -e

cd "$(dirname "$0")"

# Make the mise-managed elixir/mix/erlang available without an interactive shell.
export PATH="$HOME/.local/share/mise/shims:$PATH"

source env.sh
exec mix run --no-halt
